volumes:
  - .:/app


[host folder] : [container folder]

.     → your current project folder (on your PC)
:/app → folder INSIDE the container





icacls "C:\Users\20000449\Desktop\devops.pem" /inheritance:r /grant:r "%USERNAME%:R"

ssh -i "D:\devops\devops.pem" ubuntu@51.20.105.144


cd ~/devops

docker-compose down
docker-compose --env-file .env up -d